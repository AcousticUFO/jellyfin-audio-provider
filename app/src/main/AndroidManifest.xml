<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Background playback resilience permissions -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@android:drawable/ic_media_play"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/ic_media_play"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.JellyfinAudioProvider">

        <!-- Main User Interface (Secure: strictly the launcher activity) -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Library Sync Foreground Service (survives app switching) -->
        <service
            android:name=".sync.LibrarySyncService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Streaming Foreground Service (prevents background freezing during playback) -->
        <service
            android:name=".stream.StreamingService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Storage Access Framework Provider (Protected by MANAGE_DOCUMENTS permission) -->
        <provider
            android:name=".provider.JellyfinDocumentsProvider"
            android:authorities="@string/documents_authority"
            android:enabled="true"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
            <!-- Poweramp integration flag -->
            <meta-data
                android:name="com.maxmpz.PowerampTrackProvider"
                android:value="true" />
        </provider>

    </application>

</manifest>
